import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '80%',
    backgroundColor: '#444444',
    padding: 8,
    borderRadius: 4,
    height: 56,
    borderLeftColor: 'red',
  },
});
