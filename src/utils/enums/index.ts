export enum messageToast {
  ERROR = 'customError',
  SUCCESSFUL = 'successful',
}

export enum errorMessageFirebase {
  TOO_MANY = 'auth/too-many-requests',
  INVALID_PASSWORD = 'auth/invalid-password',
  INVALID_EMAIL = 'auth/invalid-email',
  INVALID_CREDENTIAL = 'auth/invalid-credential',
}

export enum textToast {
  ATTENTION = 'Atenção',
  SUCCESSFUL = 'Sucesso!',
}

export enum positionToast {
  POSITION = 'bottom',
}

export enum keyAsyncUser {
  USER_TOKEN_KEY = '@userToken',
}

export enum navBottomName {
  HOME = 'Home',
  REPORT = 'Report',
  MENU = 'Menu',
}
